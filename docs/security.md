# Security

Use Azure service connections or workload identity for CI authentication.
Grant Unity Catalog access through groups and the bundle permissions resource;
do not store tokens, secrets, or workspace credentials in source control.
